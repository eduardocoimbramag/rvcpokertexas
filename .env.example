# Saldo inicial de créditos virtuais (padrão: 1000)
VITE_INITIAL_BALANCE=1000

# Habilita o painel de DevTools (forçar resultados, limpar estado). Nunca habilitar em produção.
VITE_ENABLE_DEVTOOLS=false
